# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# local data lake output
_lake/

# secrets — commit .env.example, never .env
.env

# OS / editor
.DS_Store
.vscode/

# Terraform — never commit state (contains secrets) or your tfvars
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfvars
crash.log
crash.*.log
# (terraform.tfvars.example ends in .example, so it is NOT ignored)